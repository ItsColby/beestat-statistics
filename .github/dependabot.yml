version: 2
updates:
  # Update Actions automatically. Review Python pins together with the exact
  # Home Assistant and test-harness versions in each supported validation lane.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    cooldown:
      default-days: 7
